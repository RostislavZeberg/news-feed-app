import '@/styles/global.css';

import type { Metadata } from 'next';

// Экспорт константы metadata, которая содержит метаданные для страницы
// Эти данные используются для SEO и отображаются в браузере
export const metadata: Metadata = {
  title: 'News Feed App',          // Заголовок приложения (отображается во вкладке браузера)
  description: 'Infinite scroll news feed application',  // Описание приложения для SEO
};

// Экспорт компонента RootLayout по умолчанию
// Это корневой layout-компонент, который оборачивает все страницы приложения
export default function RootLayout({
  children,  // Пропс children - содержимое, которое будет вложено в layout
}: {
  children: React.ReactNode;  // Типизация пропса children как React-ноды
}) {
  return (
    // Базовый HTML-каркас приложения
    <html lang="en">  {/* Указываем язык страницы - английский */}
      <body>  {/* Тело документа */}
        {children}  {/* Вставляем содержимое страницы */}
      </body>
    </html>
  );
}

// Метаданные:

// Компонент RootLayout:
// Это корневой компонент макета, который оборачивает все страницы приложения
// Принимает пропс children (тип React.ReactNode) - это содержимое конкретной страницы
// Возвращает базовую HTML-структуру:
// <html> - корневой элемент страницы с указанием языка
// <body> - тело документа, куда вставляется содержимое страницы
// Особенности Next.js:
// Этот файл обязателен в Next.js 13+ и должен находиться в app/layout.tsx
// Он обеспечивает единую точку входа для всех страниц приложения
// Метаданные автоматически включаются во все страницы приложения
// Этот layout-компонент создает основу для всего приложения, обеспечивая:
// Единые глобальные стили
// Общие метаданные для SEO
// Базовую HTML-структуру
// Поддержку мультиязычности (через атрибут lang)